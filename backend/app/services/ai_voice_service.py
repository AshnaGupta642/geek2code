class AIVoiceService:

    def process_voice(
        self,
        patient_id: int,
        audio_url: str,
        language: str,
        conversation_id: str
    ) -> dict:

        # Member 3 / Member 4 will connect
        # the actual AI Voice / Bhashini pipeline here.

        raise NotImplementedError(
            "AI Voice service is not connected yet."
        )